# 三方審計優化建議:資訊架構 × 工程實踐 × 2026 Frontier 對齊

> 日期:2026-05-16
> 審計者:Gemini CLI (資訊架構/UX)、Codex CLI (工程實踐)、Claude Subagent (2026 frontier 對齊)
> 三家獨立、不互相看報告,以「triangulated audit」找共識項目。
> 原始三份報告:`.tmp/audit_results/gemini_report.md`、`codex_report.md`、(Claude subagent 結果見本文末附錄)。

---

## 0. 三家共識的「致命結構診斷」

三家從不同切入點得出**結構相同的結論**:

| 切入角度 | 共識診斷 |
|---|---|
| **Gemini(UX)** | 「金字塔倒置 / 華麗的空殼」——頂層架構宏大,中間教學橋樑與底層實作斷層 |
| **Codex(工程)** | 「表面完整,實際全部不擋錯」——CI/CD、依賴、安全範例都是教學筆記混 demo,沒有 package boundary |
| **Claude(內容鮮度)** | 「結構正確,但鮮度 ≠ 真實度」——22 章已覆蓋面 top-tier,但內容過度 hype 化、被識破信用即破產 |

**一句話總結**:此 repo 目前的最大風險不是「缺章節」,而是**「目錄看起來很完整,點進去卻發現是承諾未兌現的廣告」**——讀者一次性識破信用即破產。

接下來「再添加 23、24 號新主題」是錯誤策略。**先停止加內容,把已存在的東西做實**。

---

## 1. P0:三家共識的「必須立刻修」(高 ROI、低成本)

下列項目同時被 ≥2 家獨立指出,且修法簡單,**ROI 最高**,應 1-2 週內完成。

### P0-1. 全景圖黑洞(Gemini + Codex)
- **病灶**:`2024-2026_AI完整領域全景圖.md` 入度 20、出度 0,所有 11-22 章指向它,它本身的表格路徑卻是純文字、不可點擊。72% 檔案零出度。
- **修法**:`2024-2026_AI完整領域全景圖.md` Part 4 與 Part 5 表格內所有純文字目錄路徑(如 `11.AI_Hardware_Compute`)全面替換為可點擊相對連結 `[11.AI_Hardware_Compute](./11.AI_Hardware_Compute/README.md)`。
- **驗收**:`grep -c "](./11" 2024-2026_AI完整領域全景圖.md` ≥ 12。

### P0-2. CI 表面好但全部 `continue-on-error`(Codex)
- **病灶**:`.github/workflows/ci.yml` 第 58/63/68/139-140/165/172/178/232-241 行,`ruff/black/mypy/notebook/security` 全部 `continue-on-error: true` 或 `|| true`。狀態 job 沒把 `security` 納入成功條件。**等於 CI 永遠是綠的,75 個 dependabot alerts 不會 block 任何 PR**。
- **修法**:
  ```yaml
  # ci.yml:58,63,68 移除 continue-on-error
  # ci.yml:139 改成只跑 notebooks/Colab_*.ipynb,不要 --inplace,timeout=1800,失敗要紅
  # ci.yml:140 移除 || true 與 continue-on-error
  # ci.yml:232-241 status job 把 security 加入 needs
  ```
- **驗收**:故意改一行壞掉的 import,CI 必須紅。

### P0-3. 主題 2 第 6/8 章空殼 README(Gemini + Claude 都指到 Path B 斷點)
- **病灶**:`2.深入LLM模型工程與LLM運維/6.偏好對齊 (Alignment) 技術/README.md` 與 `8.模型部署與運維/README.md` 是 0 行空殼,卻是 Path B 命脈。第 6 章「偏好對齊」在第 11 章重複出現,SSOT 衝突。
- **修法**:這兩份 README 各補 50-150 行章節路由,第 6 章把 `11.現代對齊方法2024-2025/` 內重疊內容整併進來,明示「GRPO / DAPO 實作 scripts 規劃中,參考外部 TRL / verl 範例」。
- **驗收**:第 6 章 README 有 SFT→DPO/IPO/KTO/SimPO→GRPO/DAPO 完整章節大綱與外部連結。

### P0-4. 11-22 章 hype 內容三色標(Claude + Gemini 信用破產警告)
- **病灶**:11-22 章「2025-2026 重大進展」段落混用「官方確認 / 媒體報導 / agent 推測」,具體例子:RFdiffusion3「傳聞中釋出」、π0.6、Figure 03 350+、ElevenLabs v3 月份、Apple AI 晶片 2026H2、Anthropic ARR 超 OpenAI、UltraGCN NDCG +4-20%、A2UI v0.9 等。讀者掃過會誤認事實。
- **修法**:每個具體數字 / 月份 / 產品版本掛三色標籤:
  - `[Confirmed]` 有公開官方 source
  - `[Reported]` 媒體報導但未官方確認
  - `[Speculative]` agent / 業內傳言
  優先掃 11、13、14、21、22 五章(hype 密度最高)。
- **驗收**:`grep -rc "\[Reported\]\|\[Speculative\]\|\[Confirmed\]" 11.*/README.md` ≥ 5 per file。

### P0-5. 雙軌制目錄命名(Gemini)
- **病灶**:1-9 章用「`3.ML_&_Data_Analysis`」「`4.(RAG) 基礎`」(中文 + 空格 + 括號 + 符號),11-22 章用 `11.AI_Hardware_Compute`(英文蛇形)。視覺斷層 + 終端 grep / 連結地獄。並有跳號 7、8、10。
- **修法**:`git mv` 批次重命名 1-9 章成「兩碼前綴 + 英文蛇形」(`01_AI_Foundations`、`02_LLM_Engineering`...),中文名稱保留在子目錄 README H1。
- **驗收**:`ls -1 | grep -E "^[0-9]{2}_"` 列出 22 個項目,無跳號。
- **⚠️ 注意**:此項是「炸藥級」改動,會打掉所有外部連結與 git history clean degradation,建議**最後做**,並先在臨時分支驗證。

---

## 2. P1:兩家共識的「應該做」(中 ROI、要動腦)

### P1-1. 依賴版本分裂(Codex)
- **病灶**:`requirements-llm.txt:10` openai>=1.50.0,多個 demo pin `openai==1.3.0`。LangChain 主檔 >=0.3,子專案 >=0.1。`sentence-transformers` 主 3.0+/子 2.2.2。`asyncio>=3.4.3` 出現在子 requirements(asyncio 是 stdlib)。
- **修法**:
  ```bash
  pip install pip-tools
  # 新增 requirements/base.in、ml.in、llm.in、dev.in
  pip-compile --output-file=constraints.txt requirements/*.in
  # 子專案 requirements 改成 -c ../../constraints.txt
  rg -n "asyncio>=3.4.3" . -g "*requirements*.txt" --files-with-matches | xargs sed -i '/^asyncio>=/d'
  ```
- **配合**:新增 `.github/dependabot.yml` 用 grouped updates,把 75 個 alerts 收成 5-10 個 PR。

### P1-2. RAG/Agent API 安全洞(Codex)
- **病灶**:
  - `3.LLM應用工程/9.實戰/9.1-RAG-Agent端到端實戰/src/app.py:112-119` CORS fallback `["*"]`
  - `app.py:236-239` 直接用 `file.filename` 寫入 → path traversal
  - `config/config.yaml:78` `api_key_required: false` 但無 auth gate 實作
  - `src/agent_tools.py:136` `eval`、`:285` `exec`(即使有限制字串也不可靠 sandbox)
  - `3.LLM應用工程/3.Agent/examples/01_react_agent/react_agent_basic.py:101-112`、`5.AI研究前沿_2024-2025/3.Agent系統/examples/02_crewai_multi_agent.py:344-355`、`11.MCP協議與工具調用/examples/01_basic_mcp_server.py:51` 也有 `eval` / `safe_eval` 模式
- **修法**:
  - CORS 在 lifespan / config 載入後設,禁止 wildcard + credentials 同時開
  - 上傳檔名一律 `Path(file.filename).name` 或 UUID 重命名
  - `eval/exec` 改用 `ast.literal_eval` 或數學表達式專用解析(如 `simpleeval`),或預設關閉 `code_interpreter` 工具
  - 在 README 加 ⚠️ 警告「此為教學範例,生產勿用」

### P1-3. Colab notebook 浮動依賴(Codex)
- **病灶**:5 個 Colab 全是 `!pip install -U ... >=...` 浮動安裝,LangGraph/MiniGraphRAG 用 `getpass` 要 key,vLLM 硬 GPU assert。**不能算「一鍵跑」**。
- **修法**:
  - 每個 notebook install cell 改成 exact pins(`==`)或加入 `constraints-colab.txt`
  - 在 cell 開頭加「⚠️ 需要 GPU」「⚠️ 需要 OPENAI_API_KEY」明顯橫幅
  - CI workflow 只跑這 5 個 notebook、`timeout=1800`、失敗要紅(配合 P0-2)

### P1-4. SFT trainer API 過時(Codex)
- **病灶**:`2.深入LLM模型工程與LLM運維/5.監督微調 (SFT)/hands_on_project/scripts/3_train_model.py:208` 用 `evaluation_strategy`,`:216-224` 把 `dataset_text_field/max_seq_length/tokenizer` 直接傳給 `SFTTrainer`(現在 TRL 已改用 `SFTConfig` + `processing_class`)。
- **修法**:照 https://huggingface.co/docs/trl/sft_trainer 改成 SFTConfig 形式;移除 `trust_remote_code=True` 預設值,改成 explicit flag。
- **同樣問題位置**:`5.AI研究前沿_2024-2025/5.訓練與優化/examples/02_qlora_finetuning.py:75`、`3.LLM應用工程/1.LLM 部署/examples/03_huggingface_local.py:123`、`6.推論優化/1.量化技術/02_gptq_quantization.py:57,88,192,265`。

### P1-5. 入口資訊密度過載(Gemini)
- **病灶**:主 README 同時承載「個人筆記 + phantom-mesh 行銷 + 面試題庫 + 2026 前沿簡報」四個身份。
- **修法**:主 README 大刀闊斧縮減到「全景圖(指南針)+ CONCEPT_MAP + 三條入口路徑表」三塊;phantom-mesh 行銷段獨立成 `phantom-mesh-integration.md`。

### P1-6. 簡體繁體混用(Gemini)
- **病灶**:README 宣告「台灣繁體中文」,內文卻充斥「算法 / 質量 / 字符串 / 激活 / 缺省 / 內存」等簡體技術詞。
- **修法**:全局 `sed` 替換:
  ```
  算法→演算法, 質量→品質, 字符串→字串, 激活→啟動, 缺省→預設,
  內存→記憶體, 服務器→伺服器, 程序員→工程師, 進程→行程, 線程→執行緒
  ```
- **驗收**:`rg "算法|質量|字符串|激活|缺省|內存" --type md | wc -l` 收斂到 < 20。

---

## 3. P2:Claude subagent 單家觀察的「內容增量」(看時間)

這些是內容鮮度層面的建議,P0/P1 修完再做。Claude 認為 22 章雖然 top-tier,**仍系統性缺四塊**:

| 缺漏主題 | 為何補 | 建議檔案 |
|---|---|---|
| **Mechanistic Interpretability** | MIT TR 2026 Breakthrough Tech,是 22 章 RSI / 安全的真實技術底座 | `24.Mechanistic_Interpretability/README.md` + `Colab_TransformerLens_SAE_GPT2.ipynb` |
| **Agent Evaluation / 長 horizon benchmark** | τ²-bench、SWE-Bench Verified、`pass^k`,2026 H2 起企業評估事實標準 | `23.Agent_Reliability_and_Evaluation/README.md` + `Colab_Tau2Bench_Mini_Demo.ipynb` |
| **Inference Economics / Power-Constrained AI** | Omdia/Epoch:frontier 訓練放緩、small + test-time compute 反超 | `25.Inference_Economics_and_Energy/README.md`(或併入 11 章新 §13-15) |
| **AI for AI Engineering** | AlphaEvolve 在 Google 內部回收 0.7% compute、Gemini kernel +23% | `26.AI_for_AI_Engineering/README.md` |
| **Frontier RLVR / Self-Play(選配)** | NeurIPS 2025 best paper runner-up 質疑 RLVR 能否突破 base model | `27.Frontier_RLVR_and_Self_Play/README.md` + `Colab_GRPO_Math_Mini_Demo.ipynb` |

**還有 5 個可跑 notebook 應補(按 ROI 排序)**:
1. GRPO / RLVR Mini Demo(最該補,2026 H2 主流路線)
2. Agent Eval Notebook(τ²-bench 或 SWE-Bench Mini)
3. Computer Use Agent Mini
4. Voice Agent end-to-end (Pipecat + Deepgram + Cartesia)
5. AI4Science Hello-World (ColabFold + Boltz-1 或 Evo 2)

---

## 4. P3:雜項衛生(Codex)

- `.DS_Store` × 32 個被 git tracked(.gitignore 對已 tracked 無效) → `git rm --cached -r .DS_Store .coverage htmlcov .pytest_cache`
- `mkdocs.yml:153-205` nav 指向根目錄檔但 `docs_dir` 預設 `docs/` → `mkdocs build --strict` 會炸 → 改 `docs_dir: .` 或把檔搬進 `docs/`
- `docker-compose.yml:115,135,149,224,245,265` 硬編碼 password/token → 改用 `.env.example` + `${VAR}` 變數
- `GaLore_Demo/GaLore-master/setup.py:3` 讀 `requirements.txt` 但檔案不存在 → 補上或從 setup.py 拿掉
- 4 個 benchmark CI 上傳路徑與實際輸出路徑不一致(`benchmarks/results/*` vs `results/`) → 對齊一致
- `phantom-mesh` 連結偏弱:11-22 章 frontier deep-dive 與 phantom-mesh PR / commit / 程式碼幾乎零連結 → 每章末段加固定「**phantom-mesh 對應實作**」placeholder(可空,但寫死「將放在哪個目錄」)

---

## 5. 三方一致的「最終結論」

> **這份 repo 像一個華麗的建案廣告,而非一棟可以入住的房子。**(Gemini 原句)
>
> **CI 看起來完整,實際上大量檢查不擋錯;依賴版本分裂;notebook 可讀但不可嚴格重現。**(Codex 原句)
>
> **結構正確,但鮮度 ≠ 真實度。最大風險不是缺章節,而是內容過度 hype 化、被讀者一次性識破信用即破產。**(Claude subagent 原句)

**戰略建議**:
1. **凍結新主題增加**(不要追 23/24/25 章)
2. **2 週內把 P0-1, P0-2, P0-3, P0-4 修完**(全景圖實體連結 / CI 真的擋錯 / 空殼 README 補完 / hype 三色標)
3. **P0-5(目錄重命名)放最後**,先在臨時分支驗
4. **P1 視時間做,P3 一次性掃完**
5. **P2 內容增量等到 P0+P1 完成**

如果只能做一件事:**先修 P0-2(CI 擋錯)** —— 因為其他 P0 修完後,沒有 CI 把關還是會逐漸退化回去。

---

## 附錄:三份原始報告位置

- Gemini(資訊架構 / UX,77 行):`.tmp/audit_results/gemini_report.md`
- Codex(工程實踐,76 行):`.tmp/audit_results/codex_report.md`
- Claude subagent(2026 frontier 對齊,~250 行):見上一輪 Agent 完整輸出,將整理至 `.tmp/audit_results/claude_subagent_report.md`(此筆記僅摘錄共識項)

每份報告原始 prompt 在 `.tmp/{gemini,codex,claude}_audit_prompt.txt`。
